//Take a string that is a list of numbers and return the highest and lowest ones with a space between them.

using System;
using System.Linq;
using System.Collections.Generic;

public static class Kata
{
//I had help with this
  public static string HighAndLow(string numbers) => ($"{numbers.Split(' ').Select(int.Parse).Max()} {numbers.Split(' ').Select(int.Parse).Min()}");
  
}
